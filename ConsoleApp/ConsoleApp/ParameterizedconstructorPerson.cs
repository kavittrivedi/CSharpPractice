//2.Parameterized constructor
//Takes arguments to set initial state.
//3.Copy constructor(pattern)
//Takes an instance to copy values.
class ParameterizedconstructorPerson
{
    public string Name;
    public ParameterizedconstructorPerson(string name) { Name = name; }
}